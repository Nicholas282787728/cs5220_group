include("anchor_topic.jl")
driver_uci("enron")
# Load the data
#(words, Q) = load_uci("enron")

# Run the topic mining
#(p, r, A, TW) = mine_topics(Q)

# Write the results files
#write_topics("topics_enron.txt", words, p, r, A, T
